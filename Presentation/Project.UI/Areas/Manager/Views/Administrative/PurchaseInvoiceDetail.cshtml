@model PurchaseInvoiceDetailVm

@{
    ViewData["Title"] = "Alım Faturası Detayı";
    var invoice = Model.Invoice;
}

<div class="container mt-4">
    <h2>Alım Faturası Detayı</h2>
    <div class="card mb-3">
        <div class="card-body">
            <dl class="row">
                <dt class="col-sm-3">Tedarikçi</dt>
                <dd class="col-sm-9">@Model.SupplierName</dd>
                <dt class="col-sm-3">Tarih</dt>
                <dd class="col-sm-9">@Model.Invoice.OrderDate.ToShortDateString()</dd>
                <dt class="col-sm-3">Toplam Tutar</dt>
                <dd class="col-sm-9">@Model.Invoice.TotalPrice.ToString("C")</dd>
                <dt class="col-sm-3">Durum</dt>
                <dd class="col-sm-9">@Model.Invoice.OrderState</dd>
            </dl>>
        </div>
    </div>
    <h4>Fatura Kalemleri</h4>
    <table class="table table-bordered">
        <thead>
            <tr>
                <th>Ürün</th>
                <th>Miktar</th>
                <th>Birim Fiyat</th>
                <th>Durum</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var detail in Model.Details) //vm ile değişilecek
            {
                <tr>
                    <td>@detail.ProductId</td>
                    <td>@detail.Quantity</td>
                    <td>@detail.UnitPrice.ToString("C")</td>
                    <td>@detail.DetailState</td>
                </tr>
            }
        </tbody>
    </table>
    <a asp-action="PurchaseInvoiceList" class="btn btn-secondary">Geri Dön</a>
</div>